PS3GameUpdateDownloader
meant to assist in emulator use

requirements:
    python3 and pip

how to use

linux:
    python3 -m pip install -r requirements.txt
    python3 main.py
    
windows:
    py -m pip install -r requirements.txt
    py main.py
    
    OR
    grab a precompiled executable from the release pages
    
how to build the application:
    windows:
        py -m pip install -r requirements.txt
        py -m pip install pyinstaller (only for compiled version)
        
        For a compiled version:
        py build.py -c -r 
        For a version running from source:
        py build.py -s -r
        
        the finished application will be found in "dist/PS3GameUpdateDownloader"
    
    linux:
        python3 -m pip install -r requirements.txt
        python3 -m pip install pyinstaller (only for compiled version)
        
        For a compiled version:
        python3 build.py -c -r 
        For a version running from source:
        python3 build.py -s -r
        
        the finished application will be found in "dist/PS3GameUpdateDownloader"